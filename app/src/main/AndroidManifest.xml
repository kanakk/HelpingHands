<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.helpinghands">

    <application
        android:allowBackup="true"
        android:icon="@drawable/roundlogo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".organization.OrganizationalTimeLineActivity"></activity>
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".organization.OrganizationProfileActivity" />
        <activity android:name=".student.StudentProfileActivity" />
        <activity android:name=".student.FillRequestActivity" />
        <activity android:name=".organization.PostRequestActivity" />
        <activity android:name=".organization.OrganizationRegistrationActivity" />
        <activity android:name=".student.StudentRegistrationActivity" />
        <activity android:name=".RegistrationActivity" />
        <activity android:name=".MainActivity"></activity>
    </application>

</manifest>